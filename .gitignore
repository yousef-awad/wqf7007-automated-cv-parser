__pycache__/
*.py[cod]
*.ipynb_checkpoints/
.venv/
venv/
env/
.DS_Store
Thumbs.db
.env
# Ignore all data files by default
data/**
# Un-ignore the processed directory so the negation below can work
!data/processed/
# Annotated CSV is tracked via Git LFS — too expensive to regenerate (Gemini API cost)
!data/processed/resume_bio_annotated_full.csv
# Arrow files are regenerable from CSV for free (Steps 5-7 of 04_full_pipeline.ipynb)
checkpoints
exported_models
scripts
# The dashboard is its own standalone git repo (deployed to a Hugging Face Space)
dashboard/
logs/
# Local-only / sensitive — never publish
# tests/ holds real-PII sample resumes used for manual checks
tests/
.antigravitycli/
WQF7007_Project-Prposal.pdf
notebooks/02_dataset_exploration.ipynb
scratch_*.py
